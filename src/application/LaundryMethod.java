package application;

/**
 * Enum class for listing different laundry types. Better than strings because these are well-defined.
 * I already knew what enum classes were before the class, so I'm just applying that knowledge
 */
public enum LaundryMethod {
    WASHING,
    TUMBLE_DRYING,
    NATURAL_DRYING,
    IRONING,
    DRY_CLEANING,
    WET_CLEANING,
    BLEACHING
}
